/**
 * v-permission 按钮级 RBAC 权限判定指令存根
 * 职责: 根据用户权限列表动态显示或移除 DOM 元素
 */

import type { Directive } from 'vue'

export const permissionDirective: Directive = {
  mounted(el, binding) {
    // 权限校验存根
  }
}
